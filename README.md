# Arti-Chats

p2p chat over Tor

## Development

### Backend

Written in Rust, lives in `backend/`.
Uses `arti` for Tor integration.

### Frontend

Written in Kotlin, lives in `frontend/`.
Uses `androidx.compose` for UI.
